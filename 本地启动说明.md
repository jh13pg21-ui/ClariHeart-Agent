# MindBridge 本地启动说明

先启动 Ollama 和 Docker Desktop，再打开 PowerShell。

## 日常启动

```powershell
cd D:\AgentDevelop\mindbridge
& "D:\Docker\resources\bin\docker.exe" compose up -d
```

首次启动或代码、依赖发生变化时改用：

```powershell
& "D:\Docker\resources\bin\docker.exe" compose up -d --build
```

## 检查与 Harness

```powershell
& "D:\Docker\resources\bin\docker.exe" compose ps
curl.exe http://127.0.0.1:8080/actuator/health
& "D:\Docker\resources\bin\docker.exe" compose exec -T app python -m app.harness.runner
```

健康接口返回 `{"status":"UP"}` 即表示启动成功。

- MindBridge：<http://127.0.0.1:8080>
- RabbitMQ：<http://127.0.0.1:15672>

## 停止

```powershell
& "D:\Docker\resources\bin\docker.exe" compose stop
```

日常启动不需要执行 `pull`、手动数据库迁移或添加 `--build`。不要执行
`docker compose down -v`，否则会删除本地持久化数据。
