# MindBridge 本地启动说明

适用于：项目代码未改动，Docker 镜像和 Ollama 模型已经准备完成。

## 启动

1. 从 Windows 开始菜单打开 **Ollama**。
2. 打开 **Docker Desktop**，等待 Docker Engine 启动完成。
3. 打开 PowerShell，执行：

```powershell
cd D:\AgentDevelop\mindbridge
& "D:\Docker\resources\bin\docker.exe" compose up -d
```

## 检查

```powershell
& "D:\Docker\resources\bin\docker.exe" compose ps
curl.exe http://127.0.0.1:8080/actuator/health
```

健康接口返回以下内容即表示启动成功：

```json
{"status":"UP"}
```

浏览器访问：

```text
http://127.0.0.1:8080
```

- 学生账号：`student` / `student123`
- 管理员账号：`admin` / `admin123`

## 运行 Engineering Harness（可选）

```powershell
& "D:\Docker\resources\bin\docker.exe" compose exec -T app python -m app.harness.runner
```

显示 `Overall: PASS` 即表示六类工程验证全部通过。

## 停止

```powershell
& "D:\Docker\resources\bin\docker.exe" compose stop
```

日常启动不需要执行 `pull`、`ollama create` 或添加 `--build`。
