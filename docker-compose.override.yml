
  services:
    app:
      environment:
        OPENAI_BASE_URL: ${OPENAI_BASE_URL:-https://cn.crazyrouter.com/v1}
        OPENAI_API_KEY: ${OPENAI_API_KEY}
        OPENAI_EMBEDDING_MODEL: ${OPENAI_EMBEDDING_MODEL:-text-embedding-3-small}